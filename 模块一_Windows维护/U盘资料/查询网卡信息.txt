- `ethtool eth0` —— 就是这张图的内容（当前协商 / 速率 / 双工）
- `ethtool -i eth0` —— 看驱动、固件版本
- `ethtool -S eth0` —— 看网卡统计计数（收发包、丢包、错误等）
- `ethtool -p eth0` —— 让网卡口灯闪烁，用于找物理网口
- `mii-tool eth0` —— 老工具，也能看协商状态，但输出格式和这个不一样